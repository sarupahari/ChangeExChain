<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#a567df</item>
        <item name="colorPrimaryDark">#5a0da3</item>
        <item name="colorAccent">@color/cardview_light_background</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="android:textColorSecondaryInverse">@android:color/holo_purple</item>
        <item name="android:colorForeground">@android:color/black</item>
        <item name="android:textColorPrimaryInverse">@android:color/holo_purple</item>
        <item name="android:textColorSecondary">@android:color/holo_purple</item>

    </style>

</resources>
